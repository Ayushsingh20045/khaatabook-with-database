<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Hisaab</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f9f9f9;
    }

    .container {
      max-width: 800px;
      margin: 50px auto;
      padding: 20px;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
    }

    h1 {
      font-size: 2em;
      text-align: center;
      margin-bottom: 20px;
      color: #333;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .form-group {
      display: flex;
      align-items: center;
      gap: 15px;
    }

    .form-group label {
      font-size: 1em;
      font-weight: 600;
      min-width: 100px;
    }

    .form-group input {
      flex: 1;
      padding: 10px;
      font-size: 1em;
      border-radius: 8px;
      border: 1px solid #ccc;
      background-color: #f0f0f0;
      outline: none;
    }

    .form-group input:focus {
      border-color: #007bff;
    }

    #description {
      height: 100px;
    }

    .button-group {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    button {
      padding: 10px 20px;
      font-size: 1em;
      font-weight: 600;
      border: none;
      border-radius: 8px;
      cursor: pointer;
    }

    #savebtn {
      background-color: #007bff;
      color: white;
    }

    #savebtn:hover {
      background-color: #0056b3;
    }

    .cancel-btn {
      background-color: #f44336;
      color: white;
      text-decoration: none;
      display: inline-block;
      text-align: center;
    }

    .cancel-btn:hover {
      background-color: #d32f2f;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>Edit Hisaab</h1>
    <form action="/edit/<%= hisaab._id %>" method="POST">
      <!-- Name and Amount -->
      <div class="form-group">
        <label for="name">Name:</label>
        <input id="name" type="text" name="name" value="<%= hisaab.name %>" required />
      </div>

      <div class="form-group">
        <label for="amount">Amount:</label>
        <input id="amount" type="number" name="amount" value="<%= hisaab.amount %>" required />
      </div>

      <!-- Description -->
      <div class="form-group">
        <label for="description">Description:</label>
        <input id="description" type="text" name="description" value="<%= hisaab.description %>" />
      </div>

      <!-- Buttons -->
      <div class="button-group">
        <button id="savebtn" type="submit">Save Changes</button>
        <a href="/"><button class="cancel-btn">Cencel</button></a>
      </div>
    </form>
  </div>
</body>

</html>